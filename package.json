{
  "name": "cubevoyage",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "export TZ=Asia/Tokyo; wrangler types && astro dev",
    "build": "export TZ=Asia/Tokyo; wrangler types && astro build",
    "preview": "wrangler dev",
    "typecheck": "wrangler types && astro check",
    "test": "export TZ=Asia/Tokyo; vitest run",
    "test:watch": "export TZ=Asia/Tokyo; vitest",
    "astro": "astro",
    "deploy": "export TZ=Asia/Tokyo; wrangler types && astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.1.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  }
}
